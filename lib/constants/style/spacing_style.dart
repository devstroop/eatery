import 'package:flutter/material.dart';

class SpacingStyle {
  static const defaultPadding = EdgeInsets.all(12.0);
  static const defaultVerticalSpacing = SizedBox(
    height: 12.0,
  );
  static const defaultHorizontalSpacing = SizedBox(
    width: 12.0,
  );
}
